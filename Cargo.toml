[package]
name = "either"
version = "0.1.0"
authors = ["bluss"]

license = "MIT/Apache-2.0"
repository = "https://github.com/bluss/either"
documentation = "http://bluss.github.io/either"

description = "The enum `Either`, with variants `Left` and `Right` and convenient trait implementations."

keywords = ["data-structure"]
